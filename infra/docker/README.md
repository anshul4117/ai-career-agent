# Docker

Local development infrastructure using Docker Compose.

> **Status:** Scaffold placeholder — `docker-compose.yml` to be added.

## Planned Services

- PostgreSQL
- Redis

## Usage

```bash
docker compose -f infra/docker/docker-compose.yml up -d
```
